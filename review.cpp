# Create a program that asks for distance in miles as input. The program will then output how much that distance is in kilometers.

int main() {
  // Add your code below  
  std::cout << "Enter distance in miles: ";
  std::cin >> distance_miles;

  distance_kilometers;

  distance_kilometers = (0.62137 * distance_miles);

  std::cout << "The distance in kilometers is " << distance_miles << "\n";
  return distance_miles
}
